// TODO: Somehow create a handshake with the server and listen for a new event
// TODO: Websocket? But you wouldn't get the notification unless you had chrome open.

// const connection = new WebSocket("ws://localhost:9000");

// connection.addEventListener("open", () => {
//   // TODO: Send the current ids of whats in storage
//   console.log("Connected");
//   connection.send("Initial connect");
// });

// connection.addEventListener("message", (event) => {
//   const message = event.data;
//   // TODO: This will be a list of new things
//   if (message.length > 0) {
//     // something
//   }
// });
